var express = require("express")
  , router = express.Router()

  // This will handle all routes in the server.
  // router.use('/meter', require('./meter'))
  // Perhaps other meters might require other controllers.

  module.exports = router
